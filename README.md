# SV Dashboard

**Stellantis Vehicle Dashboard for Home Assistant**

This repository is being prepared as the successor to `CaneTLOTW/e_c3_dashboard`.

`main` intentionally contains no release yet. Active migration and development happen on [`develop`](../../tree/develop). A release will only be promoted to `main` after validation and explicit acceptance.
